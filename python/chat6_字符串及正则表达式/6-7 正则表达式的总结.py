r"""
一、正则表达式：又称规则表达式，是使用单个字符串来描述、匹配某个句法规则的特殊字符串，常被用来
            检索、替换那些符合某个模式(规则)的文本，即通过正则规则去验证字符串是否匹配。
    
            
二、特殊字符补充：
    1.空白字符：指的是那些在排版中用于产生空位、但不显示可见符号的字符（如空格、换行等）。
        举例：空格、\t、\n、\r(回车符)、\f(换页符)、\v(垂直制表符)、\u00A0(不换行空格)、...

    2.原始字符串：字符串前加r表示使转义字符失效从而成为原始字符串(可见6-6转义字符的第三部分)
                (建议在正则表达式中都使用r前缀方便阅读!!!)

        
三、元字符：具有特殊意义的专用字符。
    1.单字符匹配：
        (1)"."：匹配任意字符(除\n)，比如"p\nytho\tn"的匹配结果：p、y、t、h、o、\t、n；
                注：匹配点.本身用\.表示。
        (2)"\w"：匹配字母、数字、下划线，比如"python\n123"的匹配结果：p、y、t、h、o、n、1、2、3；
        (3)"\W"：匹配非字母、数字、下划线，比如"python\n123"的匹配结果：\n；
        (4)"\s"：匹配任意空白字符，比如"python\t123"的匹配结果：\t；
        (5)"\S"：匹配任意非空白字符，比如"python\t123"的匹配结果：p、y、t、h、o、n、1、2、3；
        (6)"\d"：匹配任意十进制数，比如"python\t123"的匹配结果：1、2、3；
        (7)"\D"：匹配非数字，比如"python\t123"的匹配结果：p、y、t、h、o、n、\t；

    2.数量匹配：用于限定匹配的次数(没写则默认限定一次)
        (1)"?"：匹配前面的字符0次或1次，比如"colou?r"可以匹配color或colour；
        (2)"+"：匹配前面的字符1次或多次，比如"colou+r"可以匹配colour或colouu...r；
        (3)"*"：匹配前面的字符0次或多次，比如"colou*r"可以匹配color或colou...r；
        (4)"{n}"：匹配前面的字符n次，比如"colou{2}r"可以匹配colouur；
        (5)"{n,}"：匹配前面的字符最少n次，比如"colou{2,}r"可以匹配colouur或colouuu...r；
        (6)"{n,m}"：匹配前面的字符最小n次，最多m次，
                比如"colou{2,4}r"可以匹配colouur、colouuur或colouuuur。

    3.边界匹配：
        (1)"^"：从字符串的开始位置进行匹配，相当于match方法；
        (2)"$"：从字符串的结尾位置倒着进行匹配；
            注：'^正则表达式$'表示对需要匹配的字符串s，从头到尾都要满足正则表达式的要求才能匹配；
        (3)"\b"：匹配一个字母的边界；
        (4)"\B"：匹配非字母的边界。

    4.其他匹配字符：
        (1)区间字符[]：匹配[]中列举的字符，比如[a-zA-Z0-9]表示匹配任意的大小写字母和十进制数字；
        (2)排除字符^：匹配不在[]中指定的字符，比如"[^0-9]"匹配除数字0到9字符外的字符；
        (3)选择字符|：用于匹配|左右的任意字符，比如"\d{18}|\d{15}"匹配15位身份证或18位身份证；
        (4)转义字符：同python中的转义字符，比如"\."是将 . 作为普通字符使用；
        (5)[\u4e00-\u9fa5]：匹配任意一个汉字；
    
    5.括号()：
        (1)最基本功能——捕获组：会将括号内匹配的内容捕获并保存，供后续使用
                具体使用方式：
                    1.先进行正则表达式的匹配：match = re.match(pattern, s, re.I)
                    2.通过match.group(n)，会以字符串形式输出第n个括号捕获的内容
                        注：match.groups()：会将所有捕获组以元组形式输出。
                            group(0)或 group()：整个匹配的字符串。

        (2)将括号中的字符作为一个分组，
                比如"six|fourth"是匹配six或fourth，而"(six|four)th"是匹配sixth或fourth；

        (3)非捕获组：只分组，不捕获。当只需要分组但不需要提取内容时使用，可以提高性能。
            语法：(?:pattern)  # pattern是匹配模式


三、正则表达式在python中的使用：re模块
    1.re.match(pattern,string,flags=0)：(其中pattern是匹配模式，string是要进行匹配的字符串)
        用于从字符串的开始位置进行匹配，如果起始位置匹配成功，结果为Match对象，否则为None；

    2.re.search(pattern,string,flags=0)：
        用于在整个字符串中搜索第一个匹配的值，如果匹配成功，结果为Match对象，否则为None；

    3.re.findall(pattern,string,flags=0)：
        用于在整个字符串中搜索所有符合正则表达式的值，结果是一个列表类型。
            注意：如果正则表达式里面通过括号()划分分组，则把每一个组匹配的一个结果按元组存储，构成
                列表的一个元素，最后以列表形式输出。

    4.re.sub(pattern,repl,string,count,flags=0)：(其中repl是指子串要替换成为的东西)
        用于实现对字符串中指定子串的替换；

    5.re.split(pattern,string,maxsplit,flags=0)：
        字符串中的split()方法功能相同，都是分隔字符串，输出为列表形式。


四、正则表达式的编译：用于将字符串形式的正则表达式模式编译成正则表达式对象。
    1.语法：
        pattern = re.compile(pattern_str, flags=0) 
        # 参数：pattern_str：要编译的正则表达式字符串。flags：可选，修改匹配行为的标志位
        # 返回值：返回一个re.Pattern对象

        # 注：正则表达式对象pattern，可以直接打点调用match、search、findall等方法，例如：
        pattern.search(string)   # 与上面re.search功能相同
        
    2.作用：
        (1)性能优化(主要优势)：当同一个正则表达式需要多次使用时，编译一次然后重复使用对象比每次
                        使用时重新解析字符串模式效率更高。

        (2)代码可读性和可维护性：将复杂的正则表达式命名后，代码更清晰。
"""




