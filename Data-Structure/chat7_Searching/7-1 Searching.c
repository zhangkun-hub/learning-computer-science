/*
查找(Searching)：
一、基本概念：
    1.查找表：是由一组具有相同数据类型的数据元素组成的集合，这些元素通常保存在一张结构化的数据表
        中，用于支持对其中元素的查找操作。

        类比：可以把查找表理解成一个“装着数据的仓库”，就像超市货架那样。你想找某个商品（数据），
            就要在这张“表”中查一查它在哪儿。而查找表的主要作用，就是为了方便你“找东西”。

    2.关键字：数据元素中能唯一标识该元素的属性值，查找时就是通过关键字来定位对应的数据元素的。

        类比：关键字就像“身份证号”或“商品条码”，它是用来区分每个数据的“标签”。比如你要查找学生
            信息，就得知道学号；要找图书，就要用ISBN；这些就是关键字。

    3.查找：在查找表中根据给定的“关键字”找出对应数据元素的过程，成功则返回元素位置或指针；失败则
        返回特定标识(如NULL、-1)。

    4.平均查找长度：是指在一种查找方法下，查找一个元素所需要比较的关键字的平均次数，是衡量查找算法
        效率的一个重要指标。它通常分为两种情况来讨论：查找成功和查找失败。

        计算公式：设表中有n个元素，第i个元素被查到的概率为P_i，查找它需要比较C_i次关键字
            ASL_success = ∑_i=1^n P_i*C_i


二、特点与作用
    核心特点：追求高效率，通常用平均查找长度/时间复杂度作为核心衡量指标。
    核心作用：作为数据检索的基石，高效查找是提升程序性能的关键。


三、应用场景
    数据库系统：使用B+树等索引结构加速SQL查询。
    文件与内容搜索：搜索引擎利用倒排索引快速定位包含关键词的文档。
    网络路由：路由器通过查找路由表决定数据包转发路径。
    缓存机制：键值对缓存（如Redis）通过哈希表实现极速数据存取。
    编程语言：Python字典、Java HashMap等集合类的底层实现。
    简单来说，选择哪种查找方法，取决于你的数据是否有序、是否频繁变动，以及对查找速度的要求。


四、主要查找类型：分为两大类：静态查找(数据集合不变，仅查询)和动态查找(数据集合可动态增删改查)。
1.静态查找：
    算法名称     核心思想	              时间复杂度(平均/最坏)  特点	
    顺序查找​     从数据结构起始位置开始，	 O(n) / O(n)	      优点：算法简单，对数据存储结构（顺序、链式）和有序性无要求。
                逐个元素与关键字进行比较                        缺点：效率低，数据量大时不适用。	
    
    二分查找​     在有序顺序表中，每次与	 O(log n) / O(log n)  优点：效率极高。
                中间元素比较，根据比较                          缺点：要求数据有序且必须支持随机访问（如数组），插入删除困难。
                结果将查找范围缩小一半	
                                
    插值查找​	  二分查找的改进版，根据	 O(log log n) / O(n)  优点：对于数据均匀分布的有序表，效率比二分查找更高。
                关键字在值域中的可能位                          缺点：数据分布不均匀时，效率可能降至O(n)。	
                置，自适应地确定mid点
                (按比例插值)

    斐波那契查找​ 利用斐波那契数列分割有序表，O(log n) / O(log n)  优点：只涉及加减运算，在某些场景下优于二分查找。
                根据黄金分割点确定mid位置                       缺点：需要预先计算并存储斐波那契数列。	


2.动态查找结构（适用于动态数据集）
    结构名称	 核心思想	              时间复杂度(平均/最坏)  特点	
    二叉排序树​	  要么空树，要么满足：       O(log n) / O(n)      优点：结构简单，插入删除灵活，保持了有序性。
                左子树上所有节点的值                            缺点：若插入序列有序，会退化成线性链表，查找效率降至O(n)。	
                   < 根节点的值 < 
                右子树上所有节点的值

    平衡二叉树​	  在BST基础上，通过旋转操作  O(log n) / O(log n)  优点：解决了BST可能退化的问题，保证了稳定的高效率。
                维护树的平衡(左右子树高度                       缺点：插入删除时维护平衡的算法较复杂。	
                差不超过1),如AVL树、红黑树

    B树/B+树​	 多路平衡查找树，一个节点可	 O(log n) / O(log n)  优点：专为磁盘等外存设计，一次磁盘I/O可读入多个关键字，极大减少访问磁盘次数。B+树是数据库索引的标准结构。
                拥有多个关键字和子树，大幅                      缺点：内存中维护相对复杂。	
                降低树高。B+树非叶节点仅存
                索引，叶节点链表相连

    哈希表​	     通过哈希函数将关键字直接映射 O(1) / O(n)	      优点：平均查找速度极快。
                到存储地址，理想情况下一次                      缺点：哈希冲突会影响效率；数据无序；需要良好的哈希函数和冲突处理机制。	
                访问即可找到
*/