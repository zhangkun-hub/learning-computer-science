/*
==================================== B树(即B-树) ========================================
一、设计动机与核心思想
    1. 设计动机：减少磁盘I/O
        问题：传统二叉查找树（BST、AVL树）在磁盘存储中效率低
        原因：磁盘访问以页（Page）​ 为单位（通常4KB），每次读取一个节点就是一次磁盘I/O
        目标：减少树的高度，从而减少磁盘I/O次数

    2. 核心思想
        多路分支：每个节点可以有多个子节点（远大于2）
        节点大小：每个节点的大小设计为磁盘页大小的整数倍
        平衡性：通过分裂和合并操作保持树的平衡
        局部性原理：充分利用每次磁盘I/O读取的多个数据


二、B树的定义（m阶B树）
    一棵m阶B树是满足以下性质的树：
        1.叶子节点：所有叶子节点在同一层，并且最多有m-1个关键字。

        2.非叶子节点：每个非叶子节点有k个孩子就必须有k-1个关键字(反之也是)，并且k <= m。
            (节点具体孩子数目k由以下节点类型和实际情况决定)。

        3.根节点：必须至少有1个关键字(除非是叶子节点)。

        4.内部节点：除根节点外，每个内部节点至少有⌈m/2⌉-1​个关键字(注：⌈n⌉表示将数n向上取整)。

        5.关键字与孩子排列：节点内关键字通常按升序排列，而孩子要求在关键字之间穿插，并且每个孩子的
            所有关键字要处于该孩子两端关键字范围内，即孩子的关键字对应一个"区间"。
            比如父节点关键字为17、35，则孩子T0的关键字<17，T1的关键字∈(17,35)，T2的关键字>35。
        
    注意：
        1.整棵B树的关键字按中序遍历后是有序排列的(通常升序排序)。
        2.对于3阶B树，一般称为23树，因为其节点只存在两个孩子的二节点和三个孩子的三节点。

三、B树节点结构：
    节点示例（m=5，最多4个关键字，5个子节点）：
    +---------------------------------------------------+
    | P0 | K1 | P1 | K2 | P2 | K3 | P3 | K4 | P4 |
    +---------------------------------------------------+
    其中：
    - Ki：关键字（Key）
    - Pi：指向子树的指针
    - 满足：子树P0的所有关键字 < K1 < 子树P1的所有关键字 < K2 < ...


四、B树的高度分析：(对于一棵包含n个关键字、高度为h的m阶B树)
    1.最小高度（每个节点都满）：
        根节点：1个关键字
        第1层：至少2个节点，每个节点至少⌈m/2⌉-1个关键字
        第2层：至少2⌈m/2⌉个节点
        ...
        总关键字数 n ≥ 1 + 2(⌈m/2⌉-1) + 2⌈m/2⌉(⌈m/2⌉-1) + ...
        高度 h ≤ log⌈m/2⌉((n+1)/2) + 1

    2.最大高度（每个节点关键字最少）：
        根节点：至少1个关键字，2个子节点
        其他节点：至少⌈m/2⌉-1个关键字，⌈m/2⌉个子节点
        高度 h ≤ log⌈m/2⌉(n) + 1

    3.示例：对于m=200的B树，存储2,000,000个关键字：
        最小高度：log₁₀₀(1,000,000) ≈ 3
        最大高度：log₁₀₀(2,000,000) ≈ 3.15
        实际高度通常为3-4层，查找只需3-4次磁盘I/O


五、m阶B树的各种操作操作
    1.查找操作(跟二叉查找树的思路基本一致)：
        (1)从根节点开始，逐层向下查找。
            在当前结点中，从左到右依次比较关键字：
                如果等于某个关键字，查找成功；
                如果小于某个关键字，就转入该关键字左边的子树；
                如果大于所有关键字，就转入最右边的子树。
        (2)继续在子树中重复上述过程，直到查找成功或遇到叶子节点。
            若在叶子节点中也没有找到，说明目标关键字不存在于该 B 树中；
            若在中途命中关键字，查找结束，返回该位置。
        (3)整个过程本质上是“在结点内查找 + 递归进入子树”交替进行，直到命中或结束。
            可以将 B 树的查找过程类比为“查电话本”。

    2.插入操作(比较复杂，所以只要不违反B树定义，可以自行去操作)：
        (1)查找插入位置：找到合适的叶子节点。
        (2)插入关键字：将关键字插入进去(升序排列)，如果该节点未溢出则插入操作结束，否则执行下一步。
        (3)节点分裂：
            a.中间的关键字上升到父节点。
            b.分裂产生两个子节点，平分原数据。
            c.父节点接收中间关键字及新子节点指针。
        (4)父节点若溢出，继续向上传递(递归分裂)，最终可能导致根节点分裂，此时树高增加。

    3.删除操作(非常复杂，所以只要不违反B树定义，可以自行去操作)：
        情况1：删除叶子节点的关键字
            直接删除
            如果删除后关键字数 < ⌈m/2⌉-1，需要调整
        情况2：删除内部节点的关键字
            用前驱或后继替代
                前驱：左子树的最大关键字
                后继：右子树的最小关键字
            然后删除前驱/后继（转化为情况1或3）
        情况3：合并操作
            当节点关键字数不足时：
                向兄弟节点借：如果兄弟节点有富余关键字
                与兄弟节点合并：如果兄弟节点也没有富余

        
六、B树特点总结
    1.优点：
        高度低：大大减少磁盘I/O次数
        自平衡：自动保持平衡，无需全局重组
        适合磁盘：节点大小与磁盘页匹配
        支持范围查询：中序遍历可得有序序列

    2.缺点：
        空间利用率：节点通常不满，空间利用率约50-70%
        实现复杂：插入删除需要考虑多种情况
        内存中效率不高：相比二叉查找树，内存中操作更复杂



==================================== B+树 ========================================
一、B+树与B树的核心区别
    特性	       B树	                  B+树
    数据存储​	    所有节点都存储数据	     只有叶子节点存储数据，内部节点只存索引
    叶子节点​	    叶子节点不连接	         叶子节点按大小顺序通过指针连接成有序链表
    关键字重复​     关键字不重复出现	     内部节点的关键字会重复出现在叶子节点
    查找路径​	    可能在内部节点结束	     必须到达叶子节点
    范围查询​	    需要中序遍历	         通过叶子链表高效遍历


二、B+树的结构特点(示例)：
    内部节点（索引节点）：
    +-----------------------------+
    | P0 | K1 | P1 | K2 | P2 | ... |
    +-----------------------------+
    - 只存储关键字和子节点指针
    - 关键字是子树中最大（或最小）关键字的副本

    叶子节点（数据节点）：
    +---------------------------------------------------+
    | K1 | Data1 | K2 | Data2 | ... | Kn | Datan | Next |
    +---------------------------------------------------+
    - 存储所有关键字和对应的数据（或数据指针）
    - 关键字按序排列
    - 通过Next指针连接成链表


三、B+树的特点总结
    1.优点：
        更高的扇出：内部节点只存索引，可容纳更多关键字，树更矮
        范围查询高效：叶子链表支持顺序访问
        查询稳定：所有查询都要到叶子节点，无需回溯中间节点(及递归操作)，时间复杂度稳定
        更适合磁盘：内部节点可缓存到内存，减少磁盘I/O
        全表扫描高效：通过叶子链表顺序扫描即可

    2.缺点：
        内部节点不存数据：有时需要访问叶子节点才能获取数据
        实现更复杂：需要维护叶子链表
        插入删除稍慢：需要更新叶子链表


四、B树与B+树的详细对比分析
    维度	      B树	                    B+树	                       说明
    数据存储位置​	所有节点	              仅叶子节点	                  B+树内部节点更小，扇出更大
    叶子节点连接​	无连接	                  双向链表	                      B+树范围查询效率高
    关键字重复​	    不重复	                  内部节点关键字重复出现在叶子节点	B+树保证所有数据在叶子层
    查找性能​	    不稳定(可能在内部节点找到) 稳定(必须到叶子节点)	            B树平均稍快，B+树最坏情况更好
    范围查询​	    O(log n + k)	         O(log n + k)，但常数更小	     B+树通过链表顺序访问
    内存利用​	    节点存储数据，较大	       内部节点小，可缓存更多	        B+树更适合索引
    实现复杂度​	    复杂	                  更复杂	                     B+树需要维护叶子链表
    适用场景​	    文件系统、内存结构	       磁盘存储、数据库索引	           B+树是数据库标准


五、选择建议与实际应用
    1.何时选择B树？
        文件系统：需要快速随机访问
        内存受限：数据量较小，可全部放入内存
        查询经常在内部节点结束：某些特定访问模式

    2.何时选择B+树？
        数据库索引：特别是关系型数据库
        范围查询频繁：如时间范围查询、区间查询
        全表扫描需求：需要顺序访问所有数据
        磁盘I/O敏感：需要最小化磁盘访问次数

    3.现代数据库中的B+树优化
        聚簇索引：数据按主键顺序存储
        覆盖索引：索引包含查询所需的所有字段
        复合索引：多列组合索引
        索引压缩：减少索引大小
        部分索引：只索引部分数据
*/