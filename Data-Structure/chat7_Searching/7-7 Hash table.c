/*
哈希表(Hash table)：又称散列表
一、基本概念
    关键字（Key）：要存储或查找的数据标识。
    值（Value）：与关键字关联的实际数据。
    哈希函数（Hash Function）：将关键字映射为数组下标的函数。
    哈希表（Hash Table）：存储键值对的数组(像python中的字典dict本质就是哈希表)。
    哈希冲突（Hash Collision）：不同关键字映射到同一数组下标的情况。
    装载因子（Load Factor）：已存储元素数与哈希表大小的比值。


二、核心思想与设计哲学
    1. 核心思想：哈希表的核心理念是直接地址访问：
        通过哈希函数将关键字映射到数组的直接索引位置
        理想情况下，一次计算即可定位，无需比较
        通过空间换时间，用额外的存储空间换取常数级的访问时间

    2.哈希表的工作原理：
        传统查找： 关键字 → 比较 → 比较 → ... → 找到（O(log n) 或 O(n)）
        哈希查找： 关键字 → 哈希函数 → 数组下标 → 直接访问（理想O(1)）

    3.举例说明：就像查词典：你想查“apple”，不需要一页页翻，你根据字母表顺序直接翻到以“A”开头的
        部分，这其实就是一种“散列”的思想。只不过在散列表中，是用一个“数学函数”来计算这个位置的。


二、哈希函数：
    1.定义：将关键字映射为数组下标的函数。可以把它想象成一个“指路神器”，它接收一个输入(比如学号、名
        字、手机号)，然后通过某种计算规则，把这个输入变成一个储物格编号(地址)，告诉数据该放哪。

    2.优秀哈希函数的特征
        计算快速：O(1)时间复杂度
        均匀分布：将关键字均匀映射到整个地址空间
        确定性：相同关键字总是产生相同哈希值
        抗碰撞：不同关键字尽量映射到不同位置

    3.常见哈希函数设计方法：
        (1)除留余数法（最常用）：H(key) = key % p。
            注：模数p取值很关键(根据具体情况确定)，一般建议p为一个不太接近2的幂的素数，这样可以在
                散列表中分布得更均匀，减少冲突。

        (2)数字分析法：是一种根据关键字中某些“数位”上的数字来构造散列函数的方法。
            a.核心思想：观察所有关键字在不同数位上数字的分布特征，选择分布较均匀的一位或几位，
                提取出来作为散列地址。例如，关键字是一串电话号码或学号，从中“挑几个有代表性的
                数字”来作为它在散列表中的位置

            b.操作步骤：(显然对同一组数字，数字分析法没有确定的规则和确定的哈希函数)
                收集全部关键字样本；
                统计每一位数字的分布情况；
                选择分布最“离散”的几位数字；
                将这些数字拼接/组合为散列地址。

        (3)平方取中法：
            a.核心思想：先对关键字进行平方运算，然后从所得结果中间若干位中取出一部分数字，作为
                散列地址。
                
            b.散列函数的一般形式为：H(key) = MiddleDigits(key × key)

            c.举例：假设关键字是一串数字，先让它“自乘”一遍（平方），再从这个结果的中间部分挖出
                几位作为“门牌号”（即地址）。这个方法的核心理念是，平方运算会引入更多的组合信息，
                取中间部分可以“削弱”原始数字的某些集中性，提高地址分布的均匀性。

    4.完美哈希与最小完美哈希
        完美哈希：无冲突的哈希函数（适用于静态关键字集合）
        最小完美哈希：完美哈希 + 表大小等于关键字数量
        布谷鸟哈希：使用多个哈希函数减少冲突    


三、哈希冲突：
    1.特点：哈希冲突是不可避免的（鸽巢原理）
        举例说明：如果把 n+1​ 个物体放入 n​ 个盒子中，那么至少有一个盒子里有至少两个物体。
    
    2.主要解决方法：
        (1)开放定址法（Open Addressing）：当发生冲突时，寻找下一个空闲位置。
            a.线性探测（Linear Probing）
                公式：Hi = (H(key) + i) % m (i = 1, 2,..., m-1)
                    其中m为哈希表长度，H(key)是有冲突的哈希值，Hi是重新放入的位置。
                优点：实现简单，缓存友好
                缺点：容易产生一次聚集（连续占用形成长簇）

            b.平方探测（Quadratic Probing）
                公式：Hi = (H(key) + i²) % m (i = 1, -1, 2, -2,..., m/2, -m/2)
                优点：减少聚集
                缺点：可能找不到空位置，即使表未满

            c.伪随机探测：Hi = (H(key) + i) % m，i是一个随机数

            d.双重哈希（Double Hashing）
                优点：探测序列多样化，减少聚集
                缺点：需要两个哈希函数

        (2)链地址法（Separate Chaining）：每个哈希桶是一个链表，冲突元素添加到链表中。
            a.优点：
                实现简单
                装载因子可以大于1
                删除操作简单
            b.缺点：
                需要额外指针存储空间
                缓存不友好
                链表过长时性能下降
            c.优化：链表过长时转换为红黑树（如Java 8+的HashMap）

        (3)再哈希法（Rehashing）：使用多个哈希函数，冲突时尝试下一个。  

        (4)布谷鸟哈希（Cuckoo Hashing）：使用两个哈希函数和两个表，冲突时踢出原有元素。
        

四、性能分析与时间复杂度
    1. 时间复杂度
        操作	平均情况	最坏情况	 说明
        查找​	 O(1)	    O(n)	    最坏情况：所有元素哈希到同一位置
        插入​	 O(1)	    O(n)	    包含可能的扩容开销
        删除​	 O(1)	    O(n)	    同查找
        遍历​	 O(n)	    O(n)	    需要访问所有元素

    2.空间复杂度
        存储n个元素：O(n)
        额外指针开销：链地址法额外O(n)指针空间
        装载因子影响：通常装载因子在0.5-0.75之间

    3.性能关键因素
        哈希函数质量：决定冲突频率(比如哈希函数的均匀性)。
        装载因子：影响查找时间和空间利用率。
            (装载因子：装载因子 = 已插入元素个数 / 哈希表的长度，值越大则产生冲突的可能性越大)
        冲突解决策略：影响最坏情况性能。
        扩容策略：影响动态性能。


五、优缺点总结
    1.优点
        查询速度快：平均O(1)时间复杂度
        插入删除快：平均O(1)时间复杂度
        灵活：支持任意类型的关键字
        空间效率：相比其他数据结构，空间利用率合理
        实现简单：基本思想容易理解

    2.缺点
        无序：元素没有顺序关系
        冲突处理：哈希冲突会影响性能
        空间浪费：装载因子低时空间利用率低
        哈希函数设计：需要精心设计
        最坏情况性能差：可能退化为O(n)
        不支持范围查询：只能精确查找


六、实际使用建议
    1. 选择哈希表的情况
        需要快速查找、插入、删除
        主要进行等值查询，不需要范围查询
        数据量较大，但内存充足
        关键字分布相对均匀

    2. 不选择哈希表的情况
        需要有序数据或范围查询
        内存非常紧张
        关键字分布极度不均匀
        需要稳定的最坏情况性能

    3. 实践建议
        选择合适的初始大小：减少扩容次数
        监控装载因子：保持在0.5-0.75之间
        使用优质哈希函数：如MurmurHash、CityHash
        考虑线程安全：多线程环境需要加锁或使用并发哈希表
        处理碰撞：根据场景选择链地址法或开放定址法
*/