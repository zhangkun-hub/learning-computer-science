/*
图(Graph)：图是描述“万物互联”最直观和强大的数据结构之一。数学中专门有图论来研究图的各种性质。
一、定义：图是由顶点(或节点)的集合(要求有穷非空)和连接顶点的边(或弧)的集合组成。

    1.数学表示：记为G(V,E)，其中G表示一个图，V是图G中顶点的集合，E是边的集合。
            V(G)和E(G)通常分别图G的顶点和边的集合。

    2.注意：(1)对于图这种数据机构，不允许没有顶点，但边集可以为空。
            (2)图是数据结构中用于表示多对多关系的一种非线性结构。


二、基本概念
    1.顶点：图中的基本数据单元。

    2.边：连接两个顶点并表示它们之间的关系。边可以有权重（带权图）或无权重。

    3.子图：图G的子图H，满足V(H)是V(G)的子集，且E(H)是E(G)的子集。

    4.有向图与无向图：
        边无方向的图称为无向图，数学表示为(举例)：
            顶点：V(G)={0,1,2}，边：E(G)={(0,1),(1,2),(0,2)}(其中(0,1)顺序可以颠倒)

        边有方向的图称为有向图，其中有方向的边的头称为弧头，尾称为弧尾。数学表示为(举例)：
            顶点：V(G)={0,1,2,}，边：E(G)={<0,1>,<1,0>,<1,2>}(其中<0,1>表示方向从0指向1的边)

    5.度：在无向图中，指一个顶点连接的边数；
        在有向图中，分为入度（指向该顶点的边数）和出度（从该顶点指出的边数）。

    6.权：边或顶点上关联的数值，可以代表距离、成本、容量等。

    7.网：边上带权值的图，也称带权图。

    8.路径与路径长度：从一个顶点到另一个顶点经过的顶点序列，而路径上边的条数称为路径长度。

    9.环：起点和终点是同一个顶点的路径。

    10.连通：在无向图中，如果从顶点V到顶点W有路径，则称顶点V到顶点W是连通的。

    11.连通图与非连通图：
        在无向图中，任意两个顶点之间都存在路径(即任意两顶点连通)，则该图是连通图，否则为非连通图。

    12.连通分量：无向图中的极大连通子图称为连通图。
        解释:(1)连通子图指子图为连通图。
            (2)极大指这个连通子图已经大到不能再加任何新的点了，否则就不连通了。
            (3)连通分量其实就是图中不能再扩展的"完整连通区域"，每个连通分量都是一个"最大但不能更大"
                的连通子图。

    13.强连通图：在有向图中，对于每一对顶点V和W，从V到W和从W到V都有路径，则称该有向图是强连通图。
        注：有向图中的极大强连通子图称为有向图的强连通分量，即那些不能再加的、最完整的互通子图。

    14.生成树：对于一个带权连通无向图，其生成树是它的一个子图，且该子图必须同时满足以下两个条件：
            (1)是一棵树：这意味着它无任何回路，并且无孤立顶点。
            (2)是生成树：它包含原图的所有顶点。
        (注意：对于任何一棵树，若有n个节点，则树中有n-1条边)

    15.最小生成树：权重最小：在所有满足前两个条件的生成树中，它的所有边的权重总和最小。
        理解：现实很多问题都可以抽象为：用最低的总成本，将一系列地点(顶点)连通，且保证彼此可达。

    16.简单图和多重图：
        简单图要求：1：图中不能有从顶点到其自身的边(即环)；2：同一条边在图中不能出现两次即以上。
        多重图：不满足简单图两条要求的任意一条的图称为多重图。

    17.完全图：顶点数一定，具有最多边数的简单图称为完全图(即每一对不同的顶点之间都恰有一条边相连)。


三、特点
    1.非线性：顶点间是多对多的关系，没有严格的父子或前后顺序。
    2.表示关系：核心是描述实体（顶点）之间的关联（边）。
    3.灵活性强：可以通过有向/无向、带权/无权、添加/删除顶点和边来建模各种复杂关系。
    4.包含树和线性表：树（无环连通图）和线性表（链状图）都可以看作是图的一种特殊形式。


四、重要性质
    1.度与边的关系：
        无向图中，所有顶点的度数之和等于边数的两倍。因为每条边为两个顶点各贡献一个度。
        有向图中，所有顶点的出度之和等于入度之和，弧头和弧尾的数量也相等。

    2.连通图的边数下限：一个具有 n个顶点的连通无向图至少有 n-1条边（此时它是一棵树）。

    3.完全图的边数：
        对于一个具有n个顶点的无向完全图，边数量的最大值为n(n-1)/2；
        对于一个具有n个顶点的有向完全图，边数量的最大值为n(n-1)。

    4.有向图的可达性：通过深度优先搜索或广度优先搜索等算法，可以确定从一个顶点能到达哪些其他顶点。


五、作用：图的作用是建模和解决实体间的关联问题。
    1.图的连通性：判断图中顶点是否互联互通，涉及连通分量等概念。
    2.路径与环：寻找顶点间路径、检测图中是否存在环是许多算法的基础。
    3.图的表示：主要有邻接矩阵（适合稠密图）和邻接表（适合稀疏图）两种存储方式，性能特点不同。


六、图的种类：
    分类维度	 种类	         描述
    边的方向​	  有向图	      边有方向（用箭头表示），关系是单向的（如A关注B）。
                无向图	        边无方向，关系是双向的（如A和B是好友）。

    边的权重​	  加权图	      边（或顶点）上带有表示成本的数值。
                无权图	        所有边没有权重，或视为权重相同。

    连通性​	      连通图	      无向图中任意两个顶点间都存在路径。
                非连通图	     无向图中，由多个互不连通的子图（连通分量）组成。

    特殊结构​     完全图	      每一对不同的顶点之间都恰有一条边相连。
                稀疏图/稠密图	 边数远少于/接近完全图边数的图，这是一个相对概念。
                二分图	         顶点可被分为两个集合，所有边的两个顶点分别属于这两个集合。
                有向无环图	     没有环的有向图，是描述任务依赖、编译顺序的关键结构。


七、图的应用：(无处不在，并且不止在计算机中有用)
    1.社交网络：用顶点表示用户，边表示关注或好友关系，用于推荐好友、发现社区。
    2.地图与导航：路口是顶点，道路是带权（距离、时间）的边，用于计算最短路径。
    3.网络拓扑：路由器或设备作为顶点，连接作为边，用于分析网络结构、数据传输。
    4.依赖关系：如课程先修关系、软件包依赖，用有向图表示并检测循环依赖、进行拓扑排序。
    5.状态机与推荐系统：网页链接分析、商品推荐等也基于图算法。


八、图的存储结构：(核心是如何用计算机中的数据结构来高效地存储顶点和边)
    1.顺序存储结构————邻接矩阵(Adjacency matrix)
        (1)结构：使用一个n*n的二维数组matrix来表示具有n个顶点的图。
            (a)无向图(是对称矩阵)：
                对于无权图，matrix[i][j] = 1表示顶点i到j之间有边，0表示无边。
                对于带权图，matrix[i][j]存储边的权值，用一个特殊值（如 ∞）表示无边。
            (b)有向图：
                对于无权图，matrix[i][j] = 1表示从顶点i指向j的方向上有边，0表示该方向上无边。
                对于带权图，matrix[i][j]存储方向边的权值，用一个特殊值（如 ∞）表示无边。

        (2)特点与性质：
            (a)直观易理解，逻辑关系一目了然。
            (b)空间复杂度高，为O(n²)，与边数无关。对于顶点多、边少的稀疏图，空间浪费严重。
            (c)可以方便地表示自环（查看对角线元素）。
            (d)对于无向图，矩阵是对称的，可以采用压缩存储。

        (3)优点：
            (a)查询效率高：判断任意两个顶点间是否有边，或获取边的权值，时间复杂度为O(1)。
            (b)操作简便：添加或删除一条边非常快，只需修改一个数组元素。
            (c)便于计算：某些基于矩阵运算的图算法（如计算传递闭包）实现方便。

        (4)缺点：
            (a)空间占用大：是其主要缺点，不适用于顶点数巨大的稀疏图。
            (b)添加/删除顶点开销大：需要重新分配和复制整个矩阵。
            (c)遍历邻接点效率低：要找出顶点 v的所有邻接点，需要扫描一整行，耗时O(n)，即使它只有
                很少的邻居。

        (5)应用场景：
            (a)顶点数量相对较少（n不大）的稠密图。
            (b)需要频繁判断任意两个顶点间是否存在边的场景。
            (c)适合进行矩阵运算的图算法。


    2.链式存储结构————邻接表(Adjacency list)
        (1)结构：为每个顶点建立一个单链表（或数组、动态数组）。这个链表存储了所有与该顶点直接相连的
                邻接点的信息。
            (a)对于无权图，链表节点通常只包含邻接点的编号。
            (b)对于带权图，链表节点还需包含边的权值。
            (c)对于无向图，除了从顶点指向邻接点的出边形式(即邻接表)，还有从各个顶点指向该顶点的
                入边形式(即逆邻接表)。

        (2)特点与性质：
            (a)空间利用率高，存储所有顶点和边，空间复杂度为 O(n + e)，其中 n为顶点数，e为边数。
                特别适合稀疏图。
            (b)表示方式不唯一，取决于顶点编号顺序和边的插入顺序。
            (c)对于有向图，可以方便地分出边表（邻接表）和入边表（逆邻接表）。

        (3)优点：
            (a)节省存储空间，尤其对于稀疏图。
            (b)遍历邻接点高效：查找顶点v的所有邻接点，只需遍历它的链表，时间复杂度为O(degree(v))。
            (c)添加顶点灵活：容易动态添加新的顶点。

        (4)缺点：
            (a)查询边效率较低：判断顶点 i到 j是否有边，需要遍历 i的邻接表，最坏情况 O(n)。
            (b)删除边操作稍慢：需要在链表中查找并删除节点。
            (c)实现相对复杂：需要维护多个链表结构。

        (5)应用场景：
            (a)绝大多数稀疏图的存储，这是最常用、最经典的存储方式。
            (b)需要频繁遍历某个顶点所有邻居的算法，如广度优先搜索、深度优先搜索、Dijkstra算法等。
            (c)顶点数量动态变化的图。


    3.十字链表(Orthogonal linked list)（针对有向图）
        (1)结构：是邻接表和逆邻接表的结合体，旨在高效同时获取顶点的出边和入边。
            (a)每个顶点节点维护两个指针链：以该顶点为起点的边链表（出边表）​ 和以该顶点为终点的
                边链表（入边表）。
            (b)每条有向边用一个结构体表示，该结构体存储该有向边的两个端点，并按方向从前到后存储，
                然后同时链接在“起点为某顶点的出边表”和“终点为某顶点的入边表”中。

        (2)特点与性质：
            (a)专门为优化有向图操作而设计。
            (b)空间复杂度和邻接表相同，为 O(n + e)。

        (3)优点：
            (a)同时保留了顶点的出度和入度信息，查找一个顶点的所有前驱和后继都非常高效。
            (b)避免了在邻接表中，为了查找入边需要遍历整个图的问题。

        (4)缺点：
            结构比邻接表更为复杂，构建和维护成本略高。  

        (5)应用场景：   
            (a)需要频繁同时查询顶点的出边和入边的有向图应用，如社交网络中的关注与被关注关系分析。
            (b)稀疏矩阵。


    4.邻接多重表(Adjacency multilist)（针对无向图）
        (1)结构：专门为无向图设计，解决邻接表中同一条边被存储两次（分别在两个端点的链表中）带来的
                数据不一致和操作冗余问题。
            每条边用一个节点表示，该节点同时被链接到它所关联的两个顶点的边链表中。

        (2)特点与性质：
            每条边只存储一次。
            空间复杂度和邻接表相同，为O(n + e)。

        (3)优点：
            (a)边操作更高效：删除一条边、标记一条边等操作，只需在一个边节点上进行，避免了在邻接表中
                需要修改两个链表的麻烦。
            (b)数据一致性更好。

        (4)缺点：
            结构比邻接表稍复杂。

        (5)应用场景：   
            需要对边进行频繁操作（如删除、标记）的无向图，例如在求解最小生成树的算法中。

            
    5.选择指南
        (1)当图非常稠密，或需要极快的边查询时，优先考虑邻接矩阵。
        (2)在绝大多数情况下，特别是对于稀疏图和需要遍历图的算法，邻接表是最平衡和常用的选择。
        (3)当处理有向图且特别关注顶点的入度关系时，考虑十字链表。
        (4)当处理无向图且边的删除、标记操作非常频繁时，考虑邻接多重表。

*/