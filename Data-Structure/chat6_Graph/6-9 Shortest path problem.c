/*
一、最短路径问题
    1.问题定义：最短路径问题是图论中的经典问题，指在有权图中寻找两个顶点之间总权重（或总成本、
        总距离）最小的路径。这里的"最短"指的是路径上所有边的权重之和最小。

    2.数学形式化：给定图G=(V,E)，其中V是顶点集，E是边集，每条边e∈E有权重w(e)。对于任意两个顶点
        s,t∈V，找到一条从s到t的路径P=(v₀=s, v₁, ..., vₖ=t)，使得路径的总权重∑w(vᵢ, vᵢ₊₁)最小。

    3.问题特点
        (1)多样性
            单源最短路径：从一个源点到图中所有其他顶点的最短路径
            单目标最短路径：从所有顶点到单个目标顶点的最短路径（可通过反转图转化为单源问题）
            单对顶点最短路径：指定起点和终点的最短路径
            所有顶点对最短路径：图中任意两个顶点之间的最短路径

        (2)权重类型的影响
            非负权重：可以使用Dijkstra算法
            负权重但无负环：可以使用Bellman-Ford算法
            存在负环：最短路径可能不存在（可无限减小路径长度）
            无权图：可视为所有权重为1的特殊情况

        (3)最优子结构
            最短路径问题具有最优子结构性质：如果从u到v的最短路径经过中间点w，那么从u到w和从w到v
                的子路径也分别是各自的最短路径。

        (4)松弛操作
            大多数最短路径算法基于松弛技术：不断更新当前已知最短距离估计值，直到找到真的最短路径


二、Dijkstra算法（单源，非负权重）
    1.核心思想：贪心策略，每次选择距离源点最近的未处理顶点，更新其邻居的距离。
        该思想用到的原理就是最优子结构。
    
        注意：初学者总是会走入一个误区，以为算法是用来计算从起点到终点的最短路径的。算法不是以终点
            为导向的，而是以起点为核心、全图向外扩展的策略。也就是说通过算法可以获取从起点到全图
            任意一个点的最短路径，当指定一个终点时，算法会直接告诉你从起点到终点的最短路径。它的
            目标从来不是：“我从A出发怎么尽快到达B？”而是：“我怎么从A出发，把所有点的最短路径都确
            定清楚？”

    2.算法步骤：(设源点为s)
        (1)初始化：dist[s]=0，其他dist[v]为s->v的边的权值(无边则为∞)，除s外所有顶点未访问
        (2)循环直到所有顶点已访问：
            (a)选择未访问顶点中dist最小的顶点u
            (b)标记u为已访问
            (c)对u的每个邻居v：
                如果dist[u] + w(u,v) < dist[v]：
                更新dist[v] = dist[u] + w(u,v)
                记录前驱节点

    3.时间复杂度：
        数组实现：O(V²)
        二叉堆实现：O((V+E)logV)
        斐波那契堆实现：O(E + VlogV)

    4.特点：
        仅适用于非负权重
        不能处理负环
        使用优先队列可提高效率


三、Floyd-Warshall算法（所有顶点对）
    1.核心思想：动态规划，通过中间顶点逐步优化所有顶点对之间的距离。

    2.算法步骤：
        (1)初始化：dist[i][j] = w(i,j) 如果i和j之间有边，否则∞，dist[i][i]=0
        (2)对于每个中间顶点k：
            对于每个顶点i：
                对于每个顶点j：
                    如果dist[i][k] + dist[k][j] < dist[i][j]：
                        更新dist[i][j] = dist[i][k] + dist[k][j]

    3.复杂度：
        时间复杂度：O(V³)
        空间复杂度：O(V²)

    4.特点：
        计算所有顶点对的最短路径
        可处理负权重（但不能有负环）
        实现简单，但时间复杂度高


四、各种算法对比
    算法	       类型	   权重要求	       时间复杂度	       空间复杂度  适用场景
    Dijkstra​	    单源	非负	       O((V+E)logV)	       O(V+E)	 地图导航、网络路由
    Bellman-Ford​	单源	任意（可负）    O(VE)	            O(V+E)	  金融套利检测、带负权网络
    Floyd-Warshall​	所有对	任意（无负环）	O(V³)	            O(V²)	  小规模图的所有对最短路径
    A*	           单对	   非负	          O(bᵈ)	              O(bᵈ)	    游戏AI、已知目标点的搜索
    BFS​	        单源	无权或等权	    O(V+E)	            O(V)	  社交网络距离、无权图最短路径
    SPFA​	        单源	任意（可负）	平均O(E)，最坏O(VE)	 O(V+E)	   Bellman-Ford的优化版本

    选择指南：
        非负权重图：优先选择Dijkstra算法
        含负权重图：使用Bellman-Ford算法或SPFA
        所有顶点对：小图用Floyd-Warshall，大图用多次单源算法
        已知目标点：考虑A算法*（如果有好的启发函数）
        无权图：使用BFS（时间复杂度最低）
*/