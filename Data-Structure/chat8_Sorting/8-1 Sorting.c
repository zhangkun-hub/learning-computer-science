/*
排序(Sorting)：
一、概述：排序是将一组数据按照特定顺序（如升序或降序）重新排列的过程，是数据结构与算法中的核心操作
    之一，直接影响数据检索、合并、分析等操作的效率。


二、基本概念
    1.稳定性​：若相等元素的相对顺序在排序前后保持不变，则称排序算法是稳定的(如冒泡排序)，否则为
            不稳定的(如快速排序)。
        应用场景：需按多关键字排序时（如先按分数、再按学号排序）。

    2.时间复杂度​：衡量算法执行时间随数据规模的增长趋势，包括最好、最坏、平均情况。

    3.空间复杂度​：算法运行所需的额外存储空间，
        按所需的额外存储空间分为：
        (1)原地排序（In-place）：仅需常数级额外空间（如堆排序）。
        (2)非原地排序：需额外存储空间（如归并排序）。

    5.内部排序 vs 外部排序​
        内部排序：数据全部加载到内存中进行排序。
        外部排序：数据量过大，需借助磁盘等外部存储器分段排序。

    
三、比较类排序：通过比较元素间大小决定顺序。
    1. 简单排序算法
        (1)冒泡排序​
        原理：重复交换相邻逆序元素。
        特点：稳定排序，原地排序。
        时间复杂度：O(n²)（平均/最坏），O(n)（最好，已有序时）。
        应用场景：教学示例或小规模基本有序数据。

        (2)选择排序​
        原理：每次选择未排序部分的最小元素放到已排序末尾。
        特点：不稳定排序，原地排序。
        时间复杂度：O(n²)（任何情况）。
        应用场景：教学示例或数据量小且对稳定性无要求。

        (3)插入排序​
        原理：将未排序元素插入已排序部分的正确位置。
        特点：稳定排序，原地排序。
        时间复杂度：O(n²)（平均/最坏），O(n)（最好）。
        应用场景：教学示例或小规模基本有序数据。

    2. 高效排序算法
        (1)快速排序​
            原理：分治法，选取基准将数据分为左右两部分递归排序。
            特点：不稳定排序，原地排序（递归栈空间 O(log n)）。
            时间复杂度：O(n log n)（平均），O(n²)（最坏，如已有序）。
            应用场景：通用场景中最常用的排序，尤其适合大规模随机数据。

        (2)归并排序​
            原理：分治法，将数据二分后分别排序再合并。
            特点：稳定排序，非原地排序（需 O(n) 额外空间）。
            时间复杂度：O(n log n)（任何情况）。
            应用场景：
                需要稳定排序的大数据场景。
                外部排序的基础（如多路归并）。
                链表排序（无需随机访问，空间复杂度可优化为 O(1)）。

        (3)堆排序​
            原理：将数据构建为堆，重复取出堆顶元素。
            特点：不稳定排序，原地排序。
            时间复杂度：O(n log n)（任何情况）。
            应用场景：
                对最坏时间复杂度要求高的场景。
                实时系统（如优先队列实现）。


四、非比较类排序：利用数据的特定属性（如整数范围），不依赖元素间比较。
    1.计数排序​
        原理：统计每个值的出现次数，按值顺序输出。
        特点：稳定排序，非原地排序（需 O(k) 计数数组，k 为值域范围）。
        时间复杂度：O(n + k)。
        应用场景：整数排序且值域范围较小（如年龄排序）。

    2.桶排序​
        原理：将数据分到有限数量的桶中，分别排序后合并。
        特点：稳定与否取决于桶内排序算法。
        时间复杂度：O(n + k)，k 为桶数量。
        应用场景：数据均匀分布且范围已知（如均匀分布的浮点数排序）。

    3.基数排序​
        原理：按位排序（从最低位到最高位）。
        特点：稳定排序，非原地排序（需 O(n + k) 额外空间）。
        时间复杂度：O(d · (n + k))，d 为位数。
        应用场景：多关键字排序（如手机号、字符串字典序排序）。


五、选型建议
    场景	               推荐算法
    小规模数据（n ≤ 50）	插入排序
    大规模随机数据	        快速排序
    需稳定排序	            归并排序
    数据范围有限且为整数	 计数排序
    最坏时间复杂度要求严格	 堆排序
    外部排序（数据在磁盘）	 多路归并排序
    多关键字排序	        基数排序
*/