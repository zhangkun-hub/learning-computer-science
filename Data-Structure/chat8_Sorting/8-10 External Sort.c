/*
外部排序(External sort):
一、设计原因：
    1. 问题的产生背景：外部排序主要是为了解决数据量超过内存容量时的排序问题。
        在现代数据处理中，经常会遇到以下情况：
        场景	    数据量	           内存限制	                处理需求
        大数据分析	数百GB到TB	        服务器内存通常16-256GB	 排序、聚合、连接
        数据库系统	数TB的表格	        有限的缓存池	         索引创建、查询排序
        日志处理	每日GB级别的日志	流式处理	             时间序列排序、分析
        科学计算	大规模数据集	    内存受限	             数值排序、统计

    2.内部排序的局限性（注：8-2至8-9全是内部排序）：
        (1)内部排序的要求：
            所有数据可以一次性加载到内存
            可以随机访问任何元素
            比较和交换操作代价相对较低

        (2)当数据超过内存容量时：
            无法一次性加载到内存
            磁盘I/O成为主要瓶颈
            随机访问代价高昂
            需要特殊算法处理

    3.内存与磁盘的特性差异
        特性	    内存	 硬盘（HDD）	    内存（SSD）
        访问速度	纳秒级	 毫秒级	            微秒级
        随机访问	快	     很慢（磁头寻道）	 较快
        顺序访问	很快	 较快	            很快
        成本/GB	    高	     低	                中等
        持久性	    易失	 非易失	            非易失
        关键结论：外部排序的核心是减少磁盘I/O次数，特别是减少随机I/O。


二、基本思想与算法：
    1.核心策略：分而治之(即归并排序的思想)
        (1)分阶段：将大文件分割成适合内存的小块
        (2)排序阶段：对每个小块在内存中进行排序
        (3)归并阶段：将有序的小块合并成最终有序文件

    2.算法流程示意图
        原始大文件（无序）
            ↓
        分割成多个小块
            ↓
        每个小块读入内存并排序
            ↓
        写回磁盘形成初始归并段
            ↓
        多路归并（逐步合并）
            ↓
        最终有序文件

    3.二路归并具体算法步骤：
        步骤1：生成初始归并段
            (1)输入：大文件F，内存容量M个记录
            (2)输出：R个初始归并段文件

            示例：假设：
                内存容量M = 3个记录
                输入文件：9, 4, 7, 2, 1, 8, 5, 3, 6

                第一步：读取9,4,7 → 排序后[4,7,9] → 写入run0.txt
                第二步：读取2,1,8 → 排序后[1,2,8] → 写入run1.txt
                第三步：读取5,3,6 → 排序后[3,5,6] → 写入run2.txt

                生成3个初始归并段文件：
                    run0.txt: 4,7,9
                    run1.txt: 1,2,8
                    run2.txt: 3,5,6

        步骤2：二路归并过程
            (1)将M大小的内存分成三等分作为缓冲区，其中两个为输入缓冲区，一个为输出缓冲区
                缓冲区作用：
                    磁盘I/O代价高，每次读写一个记录效率低
                    一次读写一个磁盘块（包含多个记录）更高效
                    缓冲区减少磁盘访问次数
            (2)输入：2个初始归并段文件按缓冲区大小进行切分，然后将各自每块读取到输入缓冲区中
            (3)输出：输出缓冲区为1个完全有序的数据，并写入到磁盘中

            示例：假设：
                buffer_size = 3(缓冲区大小)
                run1: [4,7,9, 10,12,15]  // 分成2个块
                run2: [1,2,8, 11,13,14]  // 分成2个块
                
                初始化：
                    buffer1 = [4,7,9]
                    buffer2 = [1,2,8]
                    out_buffer = []
                
                合并过程：
                    1. 比较4和1 → 输出1，buffer2指针移到2
                    2. 比较4和2 → 输出2，buffer2指针移到8
                    3. 比较4和8 → 输出4，buffer1指针移到7
                    4. 比较7和8 → 输出7，buffer1指针移到9
                    5. 比较9和8 → 输出8，buffer2已空
                    6. buffer2重新填充：[11,13,14]，指针重置
                    7. 比较9和11 → 输出9，buffer1已空
                    8. buffer1重新填充：[10,12,15]，指针重置
                    9. 比较10和11 → 输出10，buffer1指针移到12
                    10. 比较12和11 → 输出11，buffer2指针移到13
                    11. 比较12和13 → 输出12，buffer1指针移到15
                    12. 比较15和13 → 输出13，buffer2指针移到14
                    13. 比较15和14 → 输出14，buffer2已空
                    14. 输出buffer1剩余：[15]
                
                结果：[1,2,4,7,8,9,10,11,12,13,14,15]


三、外部排序所需时间分析
    1. 时间组成
        外部排序所需时间 = 内部排序时间(内存发生) + 归并排序时间
            (其中内部排序与归并排序都涉及到磁盘和内存的读/写时间)

    2.示例：设5400个关键字，内存大小900(即总共需要6个初始归并段，记为R1,...,R6)，缓冲区为300。
        t_IO：磁盘读/写300个关键字所需的时间
        t_s：对900个关键字进行内部排序所需的时间
        n*t_m：将n个关键字从输入缓冲区归并到输出缓冲区所需的时间

        (1)生成初始归并段时间：
            读：18*t_IO
            内部排序：6*t_s
            写：18*_IO
            总共：36*t_IO + 6*t_s

        (2)两个初始归并段进行合并的时间：
            读：6*t_IO
            输入->输出缓冲区：1800*t_m
            写：6*t_IO
            总共：12*t_IO + 1800*t_m

        (3)将6个初始归并段进行两两多次合并最终得到总有序数据的时间：
            R1(900) R2(900)              R3(900) R4(900)               R5(900) R6(900)
                ↘  ↙(12*t_IO + 1800*t_m)    ↘  ↙(12*t_IO + 1800*t_m)    ↘  ↙(12*t_IO + 1800*t_m)
                 1800                         1800                          1800
                    ↘  (24*t_I0 + 3600*t_m)  ↙                           ↙
                       ↘                  ↙                           ↙
                          ↘            ↙                           ↙
                             ↘      ↙                           ↙
                                3600                          ↙
                                   ↘ (36*t_I0 + 3600*t_m) ↙
                                      ↘                ↙
                                         ↘          ↙
                                            ↘    ↙
                                              5400
            
        总共时间为：132*t_I0 + 14400*t_m + 6*t_s


四、外部排序算法改进
    1.算法缺陷：由于外存访问速度远低于内存，一次完整的外部排序往往需要大量的磁盘读写过程，I/O时间
        通常占据了整个排序过程的绝大部分，所以需要减少I/O过程，也就是减少归并趟数。


    2.K路归并：
        (1)定义：一次性合并k个归并段（k>2）。
        (2)K路归并趟数S计算公式：S = ⌈log_k m⌉
                其中k为归并路数；m为初始归并段数；⌈x⌉表示对x进行向上取整。
        (3)提升外部排序效率：增加归并路数k，减少初始归并段个数m。
        (4)单纯K路归并的问题：k越大，在合并过程进行比较的次数也会越多，那么通过比较消耗的时间也会
            增多，所以盲目的提升k也是不可取的，这就涉及下面的多路平衡归并内容


    3.多路平衡归并————败者树
        (1)败者树定义(按升序排序为主)：
            完全二叉树：所有叶子节点在同一层。
            内部节点：存储比较中较小的元素索引(称为失败者)。
            根节点：存储k路归并的每轮比较中的最小值

        (2)败者树使用示例：
            有4个归并段：R1 = (5, 7)、R2 = (8, 10)、R3 = (3, 9)、R4 = (6, 11)

            败者树结构（完全二叉树）：
                    根节点G
                   /      \
                 内部E    内部F
                /   \     /   \
               A     B   C     D

            构建过程：
                第一轮比较：
                    1. 叶子节点：A=5, B=8, C=3, D=6
                    2. 比较A(5)和B(8) → 败者A(5) → 进入内部E
                    3. 比较C(3)和D(6) → 败者C(3) → 进入内部F
                    4. 比较E(5)和F(3) → 败者F(3) → 进入根节点G
                    5. 最小值：G(3) → 进入输出缓冲区
                第二轮比较：
                    1. 叶子节点：A=NULL, B=8, C=9, D=6
                    2. 比较C(9)和D(6) → 败者D(6) → 进入内部F
                    3. 比较E(5)和F(6) → 败者E(5) → 进入根节点G
                    4. 最小值：G(5) → 进入输出缓冲区
                第三轮比较：
                    1. 叶子节点：A=7, B=8, C=9, D=NULL
                    2. 比较A(7)和B(8) → 败者A(7) → 进入内部E
                    3. 比较E(7)和F(6) → 败者F(6) → 进入根节点G
                    4. 最小值：G(6) → 进入输出缓冲区
                第四轮比较：
                    1. 叶子节点：A=NULL, B=8, C=9, D=11
                    2. 比较C(9)和D(11) → 败者C(9) → 进入内部F
                    3. 比较E(7)和F(9) → 败者E(7) → 进入根节点G
                    4. 最小值：G(7) → 进入输出缓冲区
                第五轮比较：
                    1. 叶子节点：A=NULL, B=8, C=NULL, D=11
                    2. 比较B(8)和D(11) → 败者B(8) → 进入内部E
                    3. 比较E(8)和F(9) → 败者E(8) → 进入根节点G
                    4. 最小值：G(8) → 进入输出缓冲区
                第六轮比较：
                    1. 叶子节点：A=NULL, B=10, C=NULL, D=11
                    2. 比较B(10)和D(11) → 败者B(10) → 进入内部E
                    3. 比较E(10)和F(9) → 败者F(9) → 进入根节点G
                    4. 最小值：G(9) → 进入输出缓冲区
                第七轮比较：
                    1. 叶子节点：A=NULL, B=NULL, C=NULL, D=11
                    2. D(11) → 进入内部F
                    3. 比较E(10)和F(11) → 败者E(10) → 进入根节点G
                    4. 最小值：G(10) → 进入输出缓冲区
                第八轮比较：
                    1. 叶子节点：A=NULL, B=NULL, C=NULL, D=NULL
                    2. 内部节点：E=NULL, F=11
                    3. F(11) → 进入根节点G → 进入输出缓冲区

            最终通过败者树获取排序结果：[3, 5, 6, 7, 8, 9, 10, 11]

        (3)败者树作用：沿路径调整，只比较兄弟节点，从而减少比较次数，效率更高，


    4.置换-选择排序
        (1)问题背景：传统方法生成初始归并段时，每个归并段大小等于内存容量N。置换-选择排序可以生成
            平均长度为2N的归并段，从而减少初始归并段数量，即上述公式S = ⌈log_k m⌉的m减小。

        (2)基本思想
            1. 初始化：从输入文件读取N个记录到内存工作区
            2. 输出当前工作区中的最小值到当前归并段
            3. 从输入文件读取下一个记录：
                - 如果该记录 ≥ 刚输出的记录，加入工作区
                - 否则，放入"备用区"
            4. 重复2-3，直到工作区为空
            5. 开始新的归并段，将备用区记录移入工作区

        (3)性能分析
            理想情况：如果输入已有序，生成1个长度为N的归并段
            最坏情况：如果输入逆序，生成N/M个长度为M的归并段
            平均情况：生成约N/(2M)个长度为2M的归并段
            减少归并趟数：平均减少约50%

        (4)特点：
            生成的归并段有序。
            每个归并段长度不一定相同(所以需要按照最佳归并树进行合并)。
                 
            
    5.最佳归并树
        (1)问题背景：置换-选择排序等排序会导致归并段长度不一致，那么不同归并策略就会导致不同的
            I/O次数，所以需要研究完成所有合并所需的最小I/O次数。

        (2)基本思想
                将归并段的长度作为权重，构建一棵k叉哈夫曼树，使树的带权路径长度最小，那么按照
            哈夫曼树的合并路径，就使得完成所有合并的总I/O次数最小。而这个哈夫曼树就是最佳归并树。

        (3)最佳归并树的虚段
            a.虚段定义：因为归并段的数量不一定符合k叉哈夫曼树的构造，所以需要添加权值为0的归并段
                来参与k叉哈夫曼树的构建，这些权值为0的归并段就是虚段。当构建完成后便去掉虚段。
            
            b.虚段数量计算：
                    设k叉最佳归并树，度为k的节点个数为n_k，度为0的个数为n_0(注：哈夫曼树无其他
                度数节点)，所以按照前面树的性质可得：
                    节点总数为：n = n_0 + n_k
                    节点总度数为：k*n_k = n - 1
                于是有：
                    n_k = (n_0 - 1) / (k - 1)       
                因为n_0 = 初始归并段数目 + 虚段的数目；而n_k必须为整数，所以可以不断尝试虚段数目
                    使得n_k为满足整数要求，那么这个值就是虚段的真实数目。
*/