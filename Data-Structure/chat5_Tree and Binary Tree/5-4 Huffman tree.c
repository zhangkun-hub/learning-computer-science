/*
哈夫曼树（Huffman Tree），又称最优二叉树
一、定义：
    哈夫曼树是一种特殊的二叉树，它根据给定的权重（频率）集合构建，使得树的带权路径长度WPL最小。
        主要用于构造最优前缀编码，实现高效的数据压缩。


二、核心思想：
    给定n个权值{w₁, w₂, ..., wₙ}，构造一棵有n个叶子节点的二叉树，每个叶子节点带权值wᵢ，使得所有
        叶子节点的带权路径长度WPL最小。


三、基本概念与术语：
    概念	           解释	                                公式/示例
    路径​	            从树中一个节点到另一个节点的分支序列	节点A到节点D的路径：A→B→D
    路径长度​	        路径上的分支（边）数目	               上例中路径长度为2
    节点的带权路径长度​	 节点权值 × 根到该节点的路径长度	     权值5，路径长2，则带权路径长为10
    树的带权路径长度​	 所有叶子节点的带权路径长度之和	         WPL = Σᵢ(权值ᵢ × 路径长度ᵢ)


四、哈夫曼树的构建算法————贪心算法
    1.构建步骤：
        (1)初始化：将每个权值作为一个独立的二叉树（只有根节点），组成森林F。
        (2)选择合并：将F中的每棵树按权值从小到大排列，从中选择权值最小的两棵树。
        (3)创建新树：创建一个新节点作为这两棵树的父节点，由此构成新树，其权值为两子树权值之和。
        (4)更新森林：从F中移除这两棵树，加入新树。
        (5)重复：重复步骤2-4，直到F中只剩一棵树，那么这棵树就是哈夫曼树。

    2.构建示例：假设有字符集{A,B,C,D}，出现频率分别为{5, 3, 2, 1}
        步骤1：初始森林 [A:5, B:3, C:2, D:1]
        步骤2：选最小两个 D:1 和 C:2，合并为节点E(权值3)，森林变为 [A:5, B:3, E(权值3)]
        步骤3：选最小两个 B:3 和 E:3，合并为节点F(权值6)，森林变为 [A:5, F(权值6)]
        步骤4：选最后两个 A:5 和 F:6，合并为根节点(权值11)，得到哈夫曼树

        最终哈夫曼树结构：
                根(11)
                /    \
              A(5)   F(6)
                    /   \
                  E(3)  B(3)
                  / \   
               1(D) 2(C)


五、前缀编码与哈夫曼编码与：
    1.哈夫曼编码：
        给定一个字符串，其哈夫曼编码的构建方案：
        (1)构建带权值的叶子节点：统计字符串中每个字符出现的频率，并作为该字符的权值。

        (2)哈夫曼树的构建：按照贪心算法将这些带权值的叶子节点组成哈夫曼树。

        (3)给所有分支添加0/1数字：从根节点开始，将哈夫曼树每个度为2的节点(包括根节点)的左分支(边)
            添加0，右分支(边)添加1。

        (4)哈夫曼编码：将每个叶子节点(就是字符串中的字符)的编码设置为从根节点到该叶子节点的路径上的
            数字0/1进行排列，那么排列的结果就是该叶子节点的哈夫曼编码。

    2.前缀编码：
        (1)定义：在一个编码方案中，任意个编码都不是其他任何编码的前缀，则称编码为前缀编码。

        (2)举例：	
            0, 10, 110, 111是前缀编码
            0, 100, 110, 1110, 1100不是前缀编码(因为110是1100的前缀)        
            
        (3)哈夫曼编码与前缀编码关系：哈夫曼编码是前缀编码，无需分隔符即可解码

            
六、哈夫曼树的特点
    1. 贪心算法构建
        (1)每次合并当前权值最小的两棵树
        (2)局部最优选择导致全局最优解
        (3)构建时间复杂度：O(n log n)，使用优先队列（堆）

    2. 形态不唯一，但WPL相同
        (1)相同权值合并时顺序可能不同，导致树形态不同
        (2)但所有形态的哈夫曼树带权路径长度WPL相同且最小
        (3)权值较大的节点离根更近

    3. 没有度为1的节点
        (1)哈夫曼树是严格二叉树
        (2)每个非叶子节点都有两个子节点
        (3)n个叶子节点的哈夫曼树总共有2n-1个节点

    4. 编码特性
        (1)得到的哈夫曼编码是前缀编码，无需分隔符即可解码
        (2)频率高的字符编码短，频率低的字符编码长
        (3)平均编码长度最短


七、重要性质与数学特性
    1.节点数量关系：
        对于有n个叶子节点的哈夫曼树：
        没有度为1的节点
        度为2的节点数 = n - 1
        总节点数 = 2n - 1
        非叶子节点数 = n - 1
        
    2.WPL最优性：哈夫曼树是带权路径长度最小的二叉树。
            证明基于(具体见腾讯元宝中的计算机学习组的"数据结构中树与各种树的概念和特点"内容)：
                贪心选择性质：每次合并最小权值的树是最优选择
                最优子结构：合并后的最优解包含子问题的最优解

    3.哈夫曼树的WPL计算公式：WPL等于所有内部节点(即度为2的节点)权值之和
        设第k次合并产生的内部节点权值为m_k，则哈夫曼树的WPL为：WPL = Σ_{k=1,2,...,n-1} m_k


八、哈夫曼树的作用与应用场景
    1. 数据压缩（核心应用）：文件压缩：ZIP、GZIP、PNG等格式使用哈夫曼编码
        # 哈夫曼编码示例
        字符频率: {'A':5, 'B':3, 'C':2, 'D':1}
        哈夫曼编码: 
        A: 0    (1位)
        B: 10   (2位)  
        C: 110  (3位)
        D: 111  (3位)

        原始编码（等长）: 每个字符2位，总长 = (5+3+2+1)×2 = 22位
        哈夫曼编码总长: 5×1 + 3×2 + 2×3 + 1×3 = 5+6+6+3 = 20位
        压缩率: (22-20)/22 ≈ 9.1%

    2.图像压缩
        JPEG图像：对DCT系数进行哈夫曼编码
        PNG图像：使用DEFLATE算法（包含哈夫曼编码）

    3.通信协议
        传真机：CCITT Group 3/4标准使用改进的哈夫曼编码
        网络传输：HTTP/2使用HPACK头部压缩（基于哈夫曼编码）

    4.其他应用
        应用领域	 具体用途
        数据库系统​	  压缩存储重复数据多的列
        操作系统​	  文件系统压缩（如NTFS压缩）
        多媒体编码​	   MP3、AAC音频编码
        密码学​	      与算术编码结合用于加密
        人工智能​	  决策树构建的启发式方法


九、哈夫曼编码 vs 其他编码
    编码方式	特点	                  优点	                 缺点	                      适用场景
    哈夫曼编码​	 变长、前缀编码	            最优前缀编码，实现简单	 需要传输码表，对频率变化敏感	通用数据压缩
    算术编码​	将整个消息编码为一个分数	 更接近熵极限，压缩率高	  计算复杂，专利限制	        高压缩率需求
    LZ系列​	    字典编码，查找重复串	    无需先验统计，自适应	 解压需要字典，内存占用大	    通用文件压缩
    游程编码​	连续相同值压缩为(值,长度)	 简单高效	            只适合连续重复数据	           二值图像、简单数据
    等长编码​	每个字符固定长度	        简单，随机访问快	     压缩率低	                  未压缩格式


十、性能分析与优化
    1.时间复杂度    
        操作	       时间复杂度	 说明
        构建哈夫曼树	O(n log n)	 n为字符种类数，使用堆优化
        生成编码	    O(n)	     遍历哈夫曼树一次
        编码文本	    O(m)	     m为文本长度
        解码文本	    O(m)	     需要遍历哈夫曼树

    2.空间复杂度
        哈夫曼树：O(n)，存储2n-1个节点
        编码表：O(n)，存储每个字符的编码
        对于256个ASCII字符，n最大256，空间开销小


十一、总结与建议
    1.哈夫曼树的核心价值：
        理论意义：证明了贪心算法可以产生最优前缀编码
        实用价值：简单有效的数据压缩方法
        教学价值：理解贪心算法、二叉树、编码理论的经典案例

    2.使用建议：
        (1)适合场景：
            字符频率分布不均匀时效果最好
            需要无损压缩的场合
            资源受限的嵌入式环境

        (2)不适合场景：
            字符频率分布均匀时（压缩效果差）
            需要随机访问的数据
            数据流中频率变化剧烈（需用自适应版本）
*/