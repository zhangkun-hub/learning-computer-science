/*
二叉树的遍历(Binary tree traversal)
一、深度优先遍历(DFS)：沿着树的深度优先探索分支，尽可能深地访问节点，然后回溯，其通过递归或栈实现。
        (具体代码程序见program_1)

    1.前序遍历(Preorder)：访问顺序：根节点 → 左子树 → 右子树
        (1)具体过程：
                先访问根节点。然后访问左分支上遇到的每一个节点，持续这一过程，直到遇到空节点为止。
            这时，返回到最近的有右孩子的祖先节点，并从该节点的右孩子开始继续上述遍历过程。

        (2)应用场景：
            复制二叉树结构
            获取前缀表达式z 
            目录树的显示
            序列化二叉树

        (3)优点：
            实现简单，逻辑直观
            第一个访问的总是根节点，适合需要先知道根信息的场景

        (4)缺点：
            对于二叉搜索树，不能得到有序序列


    2.中序遍历(Inorder Traversal)：访问顺序：左子树 → 根节点 → 右子树
        (1)具体过程：
                从根节点开始，先向树的左下方移动，直到遇到空节点为止，然后访问空节点的父节点。接着
            继续遍历该节点的右子树，当右子树遍历完或没有右子树可以遍历，则继续遍历该节点的上一层
            节点，持续下去直到根节点的所有左子树访问完，然后访问根节点，最后按同样的方式访问根节点
            的右子树。

        (2)应用场景：
            二叉搜索树得到有序序列（核心应用）
            表达式树求值
            恢复二叉搜索树的结构

        (3)优点：
            对二叉搜索树能得到升序排列
            是评估表达式树的标准方式

        (4)缺点：
            实现比前序稍复杂（特别是迭代版本）


    3.后序遍历(Postorder Traversal)：访问顺序：左子树 → 右子树 → 根节点
        (1)具体过程：
            从根节点出发，先访问节点的左右儿子，再对该节点进行访问。这就意味着节点的儿子将在该节点
        之前输出。

        (2)应用场景：
            删除或释放二叉树
            计算目录大小
            获取后缀表达式（逆波兰表达式）
            计算二叉树的高度

        (3)优点：
            先处理子节点再处理父节点，适合需要自底向上计算的场景
            删除树时不会出现野指针

        (4)缺点：
            迭代实现相对复杂
            最后才访问根节点，不适用于需要先知道根信息的场景


二、广度优先遍历(BFS)：广度优先，通过队列实现
    (具体代码程序见program_2)
    1.层序遍历(Level Order Traversal)：访问顺序：从上到下，从左到右逐层访问
        (1)原理：
            从根节点开始，逐层访问
            使用队列辅助实现

        (2)应用场景：
            查找二叉树的最小深度
            判断是否为完全二叉树
            锯齿形（之字形）遍历
            二叉树的右侧视图

        (3)优点：
            直观反映树的层次结构
            适合需要按层次处理的场景
            能找到最短路径（在无权图中）

        (4)缺点：
            需要额外的队列空间
            对于深度很大的树，队列可能占用较多内存


三、高级遍历技巧————Morris遍历
    原理：利用叶子节点的空指针指向中序后继，实现O(1)空间复杂度的遍历


四、遍历方法对比表
    特性	          前序遍历	            中序遍历	         后序遍历	        层序遍历
    访问顺序​	        根左右	              左根右	           左右根	        逐层从左到右
    递归实现​	         简单	               简单	                简单	          不适用
    迭代实现​	        较简单	               中等	               较复杂	           中等
    空间复杂度​	         O(h)	               O(h)	               O(h)	              O(w)
    典型应用​	    复制树、前缀表达式	BST有序输出、表达式求值	删除树、后缀表达式	层次处理、最短路径
    第一个访问的​	    根节点	             最左叶节点	          最左叶节点	        根节点
    最后一个访问的​	 最右叶节点	              最右叶节点	         根节点	           最右下节点


五、选择指南
    使用场景	       推荐遍历方法	    理由
    复制二叉树	       前序遍历	        先创建根节点，再递归创建子树
    BST获取有序序列	   中序遍历	        自然得到升序排列
    删除二叉树	       后序遍历	        先删除子节点，再删除父节点，避免野指针
    计算节点数/高度	   后序遍历	        可以利用子树的结果计算当前节点
    判断是否为BST	   中序遍历	        检查序列是否有序
    序列化二叉树	   前序遍历	        容易重建，空节点用特殊标记表示
    找最近公共祖先	   后序遍历	        自底向上查找
    层次相关问题	   层序遍历	        自然按层次处理
    空间受限环境	   Morris遍历	    O(1)空间复杂度


六、根据遍历结果推导二叉树特性：
    1.已知前序遍历和中序遍历，可以唯一确定一棵二叉树。
    1.已知中序遍历和后序遍历，可以唯一确定一棵二叉树。
    1.已知前序遍历和后序遍历，不能唯一确定一棵二叉树。
*/
