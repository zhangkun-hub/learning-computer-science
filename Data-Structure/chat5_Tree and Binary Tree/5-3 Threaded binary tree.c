/*
(注意：线索二叉树的代码程序见program_2)
线索二叉树(Threaded binary tree)：是对普通二叉树的一种优化改进，旨在提高遍历效率和节省存储空间。
一、核心思想：
        普通二叉树有n个节点，则有2n个指针域（每个节点有left和right指针），但实际只用了n-1个指针
    （n个节点有n-1条边），剩下n+1个空指针。线索二叉树就是将这些空指针利用起来，指向在某种遍历顺序
    下的前驱节点(left空指针指向)或后继节点(right空指针指向)。


二、创建方式：
        在某种确定的遍历顺序(前序/中序/后序)下，会得到二叉树的线性排序，然后将普通二叉树中的某节点的
    空指针指向该节点的前驱(left空指针)节点和后继节点(right空指针)，从而使得遍历二叉树时可以不使用
    递归或栈，也能快速找到任意节点的前驱和后继。此外，线索二叉树还需要创建头节点，见注意2、3的内容。

    注意：
        1.虽然所有指针都利用上了，但对于某一节点，你无法分清左右指针指向的究竟是孩子节点还是
            前驱/后继节点，这就造成了混乱。

            解决方式：给结构体节点增加左右标签(ltag/rtag)，分别标注左右指针指向的到底是什么。
                比如：设置tag值为0表示指向孩子节点，1表示指向线索。

        2.头节点不存储树的数据，其左指针指向二叉树的根，右指针指向某种遍历顺序后的最后一个节点。

        3.在某种遍历顺序形成的序列中，第一个节点的左指针和最后一个节点的右指针指向头节点。


三、基本概念与术语
    1.线索​：利用空指针指向遍历顺序中的前驱或后继的指针
    2.线索化​：将二叉树改造成线索二叉树的过程
    3.线索链表​：存储线索二叉树的结构
    4.前驱节点​：在某种遍历顺序所得到的叉树的线性排序中，一个节点的前一个节点
    5.后继节点​：在某种遍历顺序所得到的叉树的线性排序中，一个节点的后一个节点
    6.标志位​：用于区分指针是指向孩子还是指向线索的标志


四、线索二叉树的类型：根据线索指向的遍历顺序不同，分为三类：
    1.前序线索二叉树
        空左指针 → 指向前序前驱
        空右指针 → 指向前序后继

    2. 中序线索二叉树（最常用）
        空左指针 → 指向中序前驱
        空右指针 → 指向中序后继

    3. 后序线索二叉树
        空左指针 → 指向后序前驱
        空右指针 → 指向后序后继


五、线索二叉树的特点：
    1.优点：
        (1)遍历速度快：可以线性时间内遍历整个树，无需递归或栈
        (2)空间利用率高：充分利用了原本浪费的空指针
        (3)前驱后继查找快：可以O(1)或O(h)时间内找到任意节点的前驱和后继
        (4)代码简洁：迭代遍历算法比递归遍历更简洁
        (5)避免栈溢出：对深度很大的树，递归可能导致栈溢出，线索二叉树可避免

    2.缺点：
        (1)插入删除复杂：插入或删除节点时需要更新线索，逻辑复杂
        (2)额外存储开销：每个节点需要增加2个标志位
        (3)实现复杂度高：比普通二叉树实现复杂
        (4)线索类型固定：一旦线索化，就只能按特定顺序快速遍历


七、线索二叉树的重要性质
    1.线索数量：n个节点的线索二叉树有n+1条线索
        证明：n个节点有2n个指针域，其中n-1个用于连接节点，剩下n+1个空指针被用作线索

    2.遍历时间复杂度：O(n)，与递归遍历相同，但空间复杂度为O(1)，优于递归的O(h)

    3.前驱后继查找：
        中序线索二叉树中，找前驱/后继平均O(1)
        普通二叉树中，找中序前驱/后继需要O(h)或O(n)

    4.存储效率：虽然每个节点多了2个标志位，但节省了遍历时的栈空间

    5.线索方向固定：一旦线索化，线索方向就固定了，不能动态改变


八、与普通二叉树的对比
    特性	       普通二叉树	          线索二叉树
    空指针利用​	    未利用，浪费	        完全利用，指向线索
    遍历方式​	    递归/栈+迭代	        沿线索直接遍历
    前驱后继查找​	需要遍历或额外指针       沿线索直接访问
    空间开销​	    2n个指针，n-1个使用  	 2n个指针全部使用
    额外存储​	    无	                    每个节点2个标志位
    插入删除​	    相对简单	            复杂，需维护线索
    遍历空间复杂度​	递归O(h)，迭代O(h)  	 O(1)
    实现难度​	    简单	                复杂


九、使用线索二叉树的场景：
    1.适用场景：
        (1)频繁遍历，特别是中序遍历
        (2)内存受限，需要节省栈空间
        (3)需要快速查找前驱后继
        (4)树结构稳定，插入删除操作少
        
    2.不适用场景：
        (1)频繁插入删除，维护线索成本高
        (2)只需要前序或后序遍历，中序线索二叉树优势不明显
        (3)树很小，递归开销可忽略
*/